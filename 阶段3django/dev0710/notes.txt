对项目操作的5个接口，有哪些痛点？
a.代码冗余非常严重，不够简洁
b.数据校验极为麻烦（往往需要嵌套多级if条件语句），校验逻辑的复用性较差
c.代码没有统一的规范，代码维护性较差
d.获取项目列表数据时，有很多功能缺失
    1）没有提供分页功能
    2）没有提供过滤功能
    3）也没有提供排序功能

e.整个项目的痛点
    1）没有提供认证授权功能
    2）没有提供限流功能
    3）传递参数形式单一，只支持json格式的参数，不支持x-www-form-urlencoded
    4）5个接口无法放在同一个类视图中

django restframwork

restful风格

