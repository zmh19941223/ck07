一、pymysql模块连接数据库

1、连接 pymysql.connect(....)

2、创建游标
    1、直接使用连接对象的curson方法创建游标  （不会自动提交事务）

    2、使用with去创建游标对象（会自动提交事务）
        with 连接对象 as  游标：
            ....
3、执行sql，调用游标对象的execute方法：
    sql = ''

4、注意点：
    使用完之后最好将游标和连接对象关闭（调用close）


二、操作数据库的类封装
    1、在init创建一个连接对象
    2、封装一个获取sql查询完之后，查询集中所有数据的方法
    3、封装一个获取sql查询完之后，查询集中第一条数据的方法
    4、封装一个获取sql查询完之后，查询到的数据条数
   注意点：
        在工作中项目中如果涉及到多个数据库，封装的时候init 方法中数据库的配置就不要从配置文件中直接读（应该参数化处理）


三、充值接口添加数据库校验
    1、充值接口调用之前，查询用户余额
    2、充值接口执行之后查询用户余额
    3、预期结果和实际结果断言通过的情况下，断言数据库中的数据变化是否正常
        判断用例是否时充值成功的用例：
            充值成功：金额变化 == 充值金额
            充值不成功：金额变化 == 0



作业：

    1、完成注册接口注册的账户数据库校验（注册成功的账户数据库中是否能查询到）
    2、完成取现接口用户余额数据库校验（）